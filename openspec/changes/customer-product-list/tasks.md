## Nhiệm vụ
1. Viết mã Cucumber Test (`.feature` và `Steps`)
2. Thêm method `getProductsByCategory` vào `CatalogControl`
3. Hardcode danh mục & sản phẩm vào `InMemoryStore`
4. Lập trình UI `ProductListUI` với FlatLaf
5. Gắn sự kiện chuyển trang vào `HomeUI` và `MainFrame`
